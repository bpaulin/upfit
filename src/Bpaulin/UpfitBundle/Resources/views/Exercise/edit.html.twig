{% extends "BpaulinUpfitBundle::layout.html.twig" %}

{% block content -%}
    <h1>Exercise edit</h1>

    <form action="{{ path('admin_exercise_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
        <input type="hidden" name="_method" value="PUT" />
        {{ form_widget(edit_form) }}
        <p>
            <button type="submit">Edit</button>
        </p>
    </form>

        <ul class="record_actions">
    <li>
        <a href="{{ path('admin_exercise') }}">
            Back to the list
        </a>
        <a href="{{ path('admin_exercise_show', { 'id': entity.id }) }}">{{ entity.id }}</a>
    </li>
</ul>
{% endblock %}
