{% extends "BpaulinUpfitBundle::layout.html.twig" %}

{% block content -%}
    <dl class="record_properties">
        {% for label, value in fields %}
            <dt class="{{ label }}">{{ label }}</dt>
            <dd class="{{ label }}">{{ value }}</dd>
        {% endfor %}
    </dl>
{% endblock %}
